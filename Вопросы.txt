Блок А
0. Начало PHP документа.
1. Какая команда используется для вывода информации.
2. Комментарии в php.
3. Правильное создание переменной.
4. Инициализация переменной.
5. Чему равна переменная $a, если $a = $b + 15. Где $b == 40;
6. Чему равна переменная $a, если $a = $a + $a. $a == 15;

Блок Б
0. Инициализация переменных(выберите правильные варианты).
1. Правильная запись условия в PHP.
2. Что будет выведено на экран?
    $a = 5;
    $b = 10;
    $res = '$a меньше $b'; 
    if($a <= $b){
        echo $res;
     };
3. Что будет выведено на экран?
    $x = 20;
    if($x < 20){
        $a = $x;
        echo $a;
    }
4. Что будет выведено на экран? 
    $x = 15; 
    $y = 45;
    if($x <= $y){
        $res = 'x меньше или равен y';
        echo $res;
    }else{ 
        $res = 'x больше y';
        echo $res;
    };

Блок С
    0. Напишите синтаксис условия. Должны сравниваться 2 переменные: $а и $b; 
    1. Напишите цикл, который будет выводить переменную $i 5 раз;
    2. Напишите программу, которая выводит переменную $a на экран;
    3. Напишите программу, которая инициализирует переменную $x;
    4. Напишите программу, которая выводит занчание персенных $a и $b;